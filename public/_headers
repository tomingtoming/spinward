# Served by Cloudflare Workers static assets. Vite content-hashes everything
# under /assets/, so those files can be cached forever; a new deploy changes
# the hash and index.html picks up the new names.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
